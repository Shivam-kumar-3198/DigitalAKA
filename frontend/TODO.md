# TODO

- [ ] Fix Tailwind import syntax in `app/globals.css` to use `@tailwind base;`, `@tailwind components;`, `@tailwind utilities;` (instead of `@import "tailwindcss";`).
- [ ] Ensure Tailwind/PostCSS configs are correct for Next.js app.
- [ ] Run `npm run build` to verify the build succeeds.

